sudo python3 -m pip install shapefile
sudo python3 -m pip install shapely
sudo python3 -m pip install numpy
sudo python3 -m pip install datetime
sudo python3 -m pip install pytz
sudo python3 -m pip install pandas --no-build-isolation
sudo python3 -m pip install statsmodels --no-build-isolation